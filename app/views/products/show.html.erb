<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Tagline:</strong>
  <%= @product.tagline %>
</p>

<%= link_to 'Edit', edit_product_path(@product), class: 'button button-outline' %>
<%= link_to 'Back', products_path, class: 'button button-clear' %>
